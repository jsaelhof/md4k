import { styled } from "@mui/material";

export const Layout = styled("div")(() => ({
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  height: 250,
  width: 250 * 0.64,
  margin: "0 auto",
}));
